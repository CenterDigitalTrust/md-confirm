def validate_c2pa(image_bytes: bytes) -> dict:
    try:
        # Demo stub representing c2pa-python Reader API logic
        # Real impl would pass bytes to c2pa.Reader(image_bytes)
        return {"c2pa_status": "missing"}
    except Exception:
        return {"c2pa_status": "missing"}
